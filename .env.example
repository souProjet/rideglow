# --- Public ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Stripe (dashboard.stripe.com/apikeys) ---
STRIPE_SECRET_KEY="sk_test_..."
# Local: `stripe listen --forward-to localhost:3000/api/webhooks/stripe` prints this.
STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Neon Postgres (orders ledger) ---
DATABASE_URL="postgresql://user:pass@ep-xxx.eu-central-1.aws.neon.tech/rideglow?sslmode=require"

# --- Resend (shipping confirmation email) ---
RESEND_API_KEY="re_..."
ORDER_EMAIL_FROM="RideGlow <commandes@example.com>"

# --- Back-office ---
# Single operator login. Generate: openssl rand -base64 32
ADMIN_PASSWORD=""
ADMIN_SESSION_SECRET=""
